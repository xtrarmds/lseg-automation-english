# Windows计划任务设置指南
## LSEG自动化程序定时执行

---

## 📋 概述

本指南将帮助你设置Windows计划任务，让LSEG自动化程序每天自动运行。我们提供了两种启动脚本：

- **启动LSEG自动化.bat** - 批处理脚本，兼容性更好
- **启动LSEG自动化.ps1** - PowerShell脚本，功能更强大

---

## 🚀 快速开始

### 方法1：双击运行（测试用）

1. **批处理脚本**：双击 `启动LSEG自动化.bat`
2. **PowerShell脚本**：右键 `启动LSEG自动化.ps1` → 选择"使用PowerShell运行"

### 方法2：Windows计划任务（推荐）

按照下面的详细步骤设置定时执行。

---

## 🛠️ 设置Windows计划任务

### 步骤1：打开任务计划程序

1. 按 `Win + R` 打开运行对话框
2. 输入 `taskschd.msc` 并按回车
3. 或者：开始菜单搜索"任务计划程序"

### 步骤2：创建基本任务

1. 在右侧操作面板点击 **"创建基本任务..."**
2. 输入任务名称：`LSEG自动化程序`
3. 输入描述：`每日自动运行LSEG数据下载和处理程序`
4. 点击 **"下一步"**

### 步骤3：设置触发器

1. 选择 **"每天"**
2. 点击 **"下一步"**
3. 设置开始时间（建议：**每天上午9:00**）
4. 设置开始日期：选择今天或明天
5. 每隔：**1天**
6. 点击 **"下一步"**

### 步骤4：设置操作

#### 选项A：使用批处理脚本（推荐新手）

1. 选择 **"启动程序"**
2. 点击 **"下一步"**
3. **程序或脚本**：点击"浏览"，选择 `启动LSEG自动化.bat`
4. **起始于**：填入脚本所在的完整路径，例如：
   ```
   C:\Users\jacky\New folder (2)
   ```
5. 点击 **"下一步"**

#### 选项B：使用PowerShell脚本（推荐高级用户）

1. 选择 **"启动程序"**
2. 点击 **"下一步"**
3. **程序或脚本**：
   ```
   powershell.exe
   ```
4. **添加参数**：
   ```
   -ExecutionPolicy Bypass -File "启动LSEG自动化.ps1" -Silent
   ```
5. **起始于**：填入脚本所在的完整路径，例如：
   ```
   C:\Users\jacky\New folder (2)
   ```
6. 点击 **"下一步"**

### 步骤5：完成设置

1. 勾选 **"当单击'完成'时，打开此任务的属性对话框"**
2. 点击 **"完成"**

### 步骤6：高级配置

在打开的属性对话框中进行以下设置：

#### 常规选项卡
- ✅ 勾选 **"不管用户是否登录都要运行"**
- ✅ 勾选 **"使用最高权限运行"**
- 配置：选择 **"Windows 10"**

#### 条件选项卡
- ❌ 取消勾选 **"只有在计算机使用交流电源时才启动此任务"**
- ✅ 勾选 **"如果计算机由电池供电，停止任务"** (笔记本电脑可选)

#### 设置选项卡
- ✅ 勾选 **"允许按需运行任务"**
- ✅ 勾选 **"如果过了计划开始时间，立即启动任务"**
- ✅ 勾选 **"如果任务失败，每隔以下时间重新启动"**：**10分钟**，**尝试3次**
- ❌ 取消勾选 **"如果请求后任务还在运行，强行将其停止"**

#### 触发器选项卡（可选：设置重试）
1. 选择触发器，点击 **"编辑"**
2. 点击 **"高级设置"**
3. ✅ 勾选 **"重复任务间隔"**：**30分钟**，持续时间：**2小时**
4. 这样如果首次运行失败，会在30分钟后重试

---

## 📊 启动脚本功能对比

| 功能 | 批处理脚本 (.bat) | PowerShell脚本 (.ps1) |
|------|------------------|----------------------|
| **兼容性** | ✅ 所有Windows版本 | ✅ Windows 7+ |
| **彩色输出** | ❌ 基础文本 | ✅ 彩色文本 |
| **错误处理** | ✅ 基础处理 | ✅ 高级处理 |
| **参数支持** | ❌ 无 | ✅ 支持参数 |
| **强制执行** | ❌ 无 | ✅ -Force参数 |
| **静默模式** | ❌ 无 | ✅ -Silent参数 |
| **执行时间统计** | ❌ 无 | ✅ 有 |
| **启动日志** | ✅ 基础日志 | ✅ 详细日志 |

---

## 🧪 测试计划任务

### 立即运行测试

1. 在任务计划程序中找到你创建的任务
2. 右键任务 → 选择 **"运行"**
3. 观察任务状态变化
4. 检查 `logs\startup_YYYYMMDD.log` 文件

### 检查执行历史

1. 选择你的任务
2. 点击 **"历史记录"** 选项卡
3. 查看任务执行记录和结果

### 查看运行结果

1. **启动日志**：`logs\startup_YYYYMMDD.log`
2. **程序日志**：`logs\lseg_automation_YYYYMMDD.log`
3. **Windows事件日志**：事件查看器 → Windows日志 → 应用程序

---

## 🔧 常见问题解决

### 问题1：任务显示"正在运行"但实际未执行

**原因**：权限不足或路径错误

**解决方案**：
1. 确保勾选了"使用最高权限运行"
2. 检查"起始于"路径是否正确
3. 使用完整的绝对路径

### 问题2：PowerShell脚本无法执行

**原因**：执行策略限制

**解决方案**：
1. 以管理员身份打开PowerShell
2. 运行：`Set-ExecutionPolicy RemoteSigned`
3. 或在计划任务中使用 `-ExecutionPolicy Bypass` 参数

### 问题3：任务启动但立即结束

**原因**：Python环境变量或依赖包问题

**解决方案**：
1. 检查Python是否在系统PATH中
2. 确保所有依赖包已安装
3. 查看启动日志获取详细错误信息

### 问题4：在锁定状态下任务不运行

**原因**：用户会话限制

**解决方案**：
1. 确保勾选了"不管用户是否登录都要运行"
2. 输入用户密码进行身份验证
3. 取消勾选"只有在计算机使用交流电源时才启动此任务"

---

## 📈 监控和维护

### 日志监控

定期检查以下日志文件：

1. **启动日志**：`logs\startup_YYYYMMDD.log`
   - 记录脚本启动和基础信息
   - 记录Python环境检查结果
   - 记录程序退出代码

2. **程序日志**：`logs\lseg_automation_YYYYMMDD.log`
   - 详细的程序执行过程
   - 登录状态、下载进度、处理结果
   - 错误信息和异常处理

### 日志清理

建议设置日志清理机制：

#### 手动清理（每月）
```powershell
# 删除30天前的日志文件
Get-ChildItem logs\*.log | Where-Object {$_.LastWriteTime -lt (Get-Date).AddDays(-30)} | Remove-Item
```

#### 自动清理（创建额外的计划任务）
1. 创建新任务："清理LSEG日志"
2. 频率：每月第一天
3. 操作：运行上述PowerShell命令

### 性能监控

关注以下指标：

- **执行时间**：正常情况下5-10分钟
- **成功率**：应该接近100%
- **重复执行**：每天只应执行一次
- **文件大小**：SFTP上传的文件大小是否正常

---

## 🚨 重要提醒

### 安全注意事项

1. **密码保护**：config.cnf包含敏感信息，确保文件权限安全
2. **网络安全**：定期更新SFTP和邮箱密码
3. **系统更新**：保持Windows和Python版本更新

### 备份建议

1. **配置备份**：定期备份config.cnf文件
2. **脚本备份**：保存所有脚本文件的副本
3. **日志备份**：重要的日志文件应备份保存

### 故障恢复

1. **程序失败**：检查日志，修复配置或网络问题
2. **任务失效**：重新创建计划任务
3. **系统重启**：确认任务在重启后仍能正常运行

---

## 🎯 总结

通过设置Windows计划任务，LSEG自动化程序可以：

✅ **每天自动运行**：无需人工干预  
✅ **智能跳过重复**：避免重复执行  
✅ **完整日志记录**：便于问题追踪  
✅ **错误自动重试**：提高成功率  
✅ **灵活配置选项**：适应不同需求  

设置完成后，程序将每天按时自动执行，为你提供可靠的数据处理服务！🎉

---

*最后更新时间：2024年12月*
*如有问题，请检查日志文件或联系技术支持* 